const a = 28;
const b = 52;

/* Adição */
let adicao = a + b;

/* Subtração */
let subtracao = a - b;

/* Multiplicação */
let multiplicacao = a * b;

/* Divisão */
let divisao = a / b;

/* Módulo */
let modulo = a % b;

/* Retornando o maior valor entre dois numeros */
if (a > b) {
    console.log(a);
}
else if (a < b) {
    console.log(b);
}
