const a = 28;
const b = 52;
const c = 87;

/* Adição */
let adicao = a + b;

/* Subtração */
let subtracao = a - b;

/* Multiplicação */
let multiplicacao = a * b;

/* Divisão */
let divisao = a / b;

/* Módulo */
let modulo = a % b;

/* Retornando o maior valor entre dois numeros */
if (a > b) {
    console.log(a);
}
else if (a < b) {
    console.log(b);
}

/* Retornando o maior de três números */
if (a > b && a > c) {
    console.log(a);
}
else if (b > a && b > c) {
    console.log(b);
}
else if (c > a && c > b) {
    console.log(c);
}
